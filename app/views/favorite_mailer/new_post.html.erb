<!DOCTYPE html>
<html>
    <head>
        <meta content="text.html; charset=UTF-8" http-equiv="Content-Type" />
    </head>
    <body>
        <h1>You've created "<%= @post.title %>"</h1>
        <small>
            By: <%= @post.user.name %>
        </small>
        <p>
            <%= @post.body %>
        </p>
        <p>
            <%= link_to "View Post on site", topic_post_url(@post.topic, @post, anchor: "post-#{@post.id}") %>
        </p>
    </body>
</html>